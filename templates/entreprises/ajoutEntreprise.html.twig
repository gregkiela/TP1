{% extends 'base.html.twig' %}

{% block title %}{{parent()}} d'accueil !{% endblock %}
{% block header %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; text-align: center }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Ajouter une Entreprise</h1>
    </div>
{% endblock %}
{% block body %}
<div class="container-fluid">
    <div class="row">       
        <div class="col-md-8 offset-2" >
            <div class="card"> 
                {{ form_start (vueFormulaireEntreprise)}}
                    {{ form_row (vueFormulaireEntreprise.nom)}}
                    {{ form_row (vueFormulaireEntreprise.activite)}}
                    {{ form_row (vueFormulaireEntreprise.adresse)}}
                    {{ form_row (vueFormulaireEntreprise.urlSite)}}
                    <button type="submit" class="btn-dark">Ajouter</button>
                {{ form_end (vueFormulaireEntreprise)}}
            </div>
        </div>
    </div>
</div>
{% endblock %}
