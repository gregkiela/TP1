{% extends 'base.html.twig' %}

{% block title %}{{parent()}}{% endblock %}
{% block header %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Liste des stages triés par l'entreprises {{entreprise.nom}}</h1>
    </div>
{% endblock %}
{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            {% block stages %} 
                {% for stage in stages %}
                    <div class="card">
                        <h5 class="card-header">    
                            <a href=.././stages/{{stage.id}})>{{stage.titre}}</a>
                        </h5>
                        <div class="card-body">
                            <p class="card-text"></p>
                        </div>                        
                    </div>                
                {% endfor %}
            {% endblock %}
        </div>
    </div>
</div>
    <div>
        <a href={{path('accueil')}}>Revenir à l'accueil</a>
    </div>
{% endblock %}